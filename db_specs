message {
	id,
	type,
	author,
	data
}

users {
	username,
	hash
}

dict() = user_ip {
	username,
	user_ip
}

backlog {
	user_id
	message_id,
}

group {
	group_id,
	group_name
}

group_user {
	group_id,
	user_id
}

-------------

data {
	type,
	data,
	author,
	destination: { 'user': ['username', ...] , 'group': ['groupname', ...] }
} 
